<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.opennms.gsoc"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:targetSdkVersion="14" android:minSdkVersion="10" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".OpenNMSAndroidAppActivity"
            android:label="@string/app_name" android:theme="@style/Theme.Sherlock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <service android:name=".nodes.NodesService"></service>
        
        <activity android:name=".nodes.NodesActivity" android:label="@string/app_name"
                        android:theme="@style/Theme.Sherlock">
        </activity>
        <activity android:name=".about.AboutActivity" android:label="@string/app_name"
                        android:theme="@style/Theme.Sherlock">
        </activity>
    </application>
	<!-- android:theme="@android:style/Theme.NoTitleBar" -->
</manifest>